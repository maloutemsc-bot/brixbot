Flask==3.0.3
Flask-SQLAlchemy==3.1.1
Flask-Cors==4.0.1
Flask-Limiter==3.8.0
requests==2.32.3
# rule34_service (.nsfw) : parsing HTML direct avec requests SEUL (voir le
# docstring du service). PAS de reliq/treerequests : reliq est un wrapper C
# compilé (libreliq.so) impossible à installer sur Termux/Android.
python-dotenv==1.0.1
gunicorn==22.0.0
# Pillow : conversion image → sticker WebP (fallback .sticker quand sharp est
# indisponible, ex: Termux/Android). Encodage WebP nativement supporté.
Pillow==10.4.0
# fontTools : conversion woff→ttf de la police Arial Narrow (.brat authentique,
# téléchargée à l'installation via fetch_brat_font.py — jamais committée).
fonttools==4.53.1
# Playwright : scraping du VRAI générateur brat (bratify.vercel.app) pour un
# rendu 100% identique au site. Utilise le navigateur SYSTÈME (Chrome/Edge/
# Chromium), aucun téléchargement Chromium requis. Optionnel : si absent, .brat
# retombe sur la génération locale brat_service.py.
playwright==1.46.0
# shazamio : reconnaissance musicale (.shazam) — API Shazam reverse-engineered,
# gratuite sans clé. Le décodage ogg→wav est fait côté bot (ffmpeg-static).
shazamio==0.8.1
